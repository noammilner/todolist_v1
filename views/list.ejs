<!DOCTYPE html>

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>To Do List</title>
    <link rel="stylesheet" href="css/styles.css" />
  </head>

  <body>
    <div class="box" id="heading">
      <h1><%= listTitle %></h1>
    </div>

    <div class="box">
      <% for (i = 0; i < newListItems.length; i++){ %>
      <div class="item">
        <input type="checkbox" />
        <p><%= newListItems[i] %></p>
      </div>
      <% } %>

      <form class="" action="/" method="post">
        <input
          type="text"
          name="newTask"
          id=""
          placeholder="New Item"
          autocomplete="off"
        />
        <button type="sumbit" name="list" value="<%= listTitle %>">+</button>
      </form>
    </div>
  </body>
  <footer>Copyright 2022 Noam Milner</footer>
</html>
